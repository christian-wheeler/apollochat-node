{
  "name": "apollochat",
  "version": "1.0.0",
  "description": "Apollo GraphQL chat server that leverages Firebase",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && node lib/index.js",
    "start": "node lib/index.js",
    "deploy": "npm run build && gcloud app deploy"
  },
  "main": "lib/index.js",
  "author": "Christian Wheeler",
  "license": "MIT",
  "dependencies": {
    "apollo-server": "^2.3.1",
    "firebase-admin": "^6.5.0",
    "graphql": "^14.1.1",
    "graphql-tag": "^2.10.0"
  },
  "devDependencies": {
    "@types/graphql": "^14.0.5",
    "tslint": "^5.12.1",
    "typescript": "^3.2.2"
  }
}
